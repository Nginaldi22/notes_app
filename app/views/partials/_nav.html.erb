<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
 <div class="container-fluid">
     <ul class="navbar-nav me-auto mb-2 mb-lg-0">
     <% if current_user %>
     <li><%= link_to "Homepage", homepage_show_path(current_user.id), class: "nav-link text-white"%>
     <li><%= button_to "Log out", logout_path, method: :delete %>
    <%else%>
    <li><%= link_to "Login", root_path, class: "nav-link text-white"%>
    <li><%= link_to "Sign Up", registration_new_path, class: "nav-link text-white"%>
    <%end%>
    </ul>
 </div>
</nav>
